# 미결 질문

진웅이 답해야 풀리는 것들. 에이전트는 여기에 질문을 추가하고,
**그 답에 의존하지 않는 작업은 먼저 끝낸다.** 답이 나오면 `03-decisions.md`로 옮긴다.

---

## 추천 (지금 트랙)

### ~~Q1. 팝업 데이터를 어떻게 채울 것인가~~ → 답 나옴 (`03-decisions.md` #13)

공공 API로 기본 데이터를 만들고, 팝업은 검색·수동 큐레이션으로 보강한다.
Instagram 화이트리스트는 1단계에서 뺀다.
(`../reference/data/date-course-data-strategy.md`)

### ~~Q2. 베타에서 추천을 실제로 켤 것인가~~ → 답 나옴 (`03-decisions.md` #18)

친구 2~3커플에게 "아직 실험 중"이라고 알리고 연다.

### ~~Q3. 취향 온보딩을 넣을 것인가~~ → 홈 핵심 취향으로 다시 결정 (`03-decisions.md` #30)

2026-08-11에 다시 열었다가 같은 날 닫았다. 문항이 전부 *속성*(음식 종류·실내외·
분위기)을 물었는데, 그건 순위 매기기 문제의 입력이다. 하루 코스는 일정 짜기
문제라 필요한 입력이 다르다 — 언제, 어디서, 얼마.

가입 과정의 온보딩은 넣지 않는다. 대신 연결 후 홈 최상단에서 실제 장소 패싯과 바로
매칭되는 20문항을 각자 필수로 받는다. 코스 반응과 오늘의 질문은 이후 맥락 신호로 계속
쌓되, 첫 코스가 근거 없는 자동 배치가 되지 않게 핵심 취향을 기준선으로 둔다.

### Q14. 알레르기를 필터할 근거가 장소 쪽에 없다 — **안전 문제다**

절대 규칙 5는 알레르기를 하드 필터로 못박았다. 그런데 적재된 음식점 565건 중
무엇에 땅콩이 들어가는지 알 방법이 **전혀 없다.** 메뉴 데이터를 주는 공개 API가
없고, 카카오 로컬도 `category_name`(`음식점 > 한식 > 육류,고기`)까지만 준다.

**물어보고 거르지 못하는 건 안 물어보는 것보다 나쁘다.** 지킬 수 없는 안전
약속이 된다. 세 갈래가 있다.

1. 알레르기를 아예 묻지 않는다 — 정직하지만 제품 약속을 축소한다
2. 카테고리 수준으로만 거른다 — 해산물 알레르기 → 횟집·해물 카테고리 제외.
   부분적이지만 실행 가능하다. 견과류처럼 카테고리에 안 드러나는 건 여전히 못 거른다
3. 입력받되 "확인하지 못했다"를 코스에 표시한다 — 책임을 사용자에게 넘긴다

Q11(반려동물 동반)이 같은 모양이다. 정보가 '미상'인 장소를 어떻게 다룰지가
두 질문의 공통 뼈대다.

**하루 코스가 되면서 이 질문의 무게가 달라졌다.** 코스는 "12:00 이 식당"이라고
콕 집어 말한다. 목록에서 하나 고르라는 것보다 훨씬 강한 권유라, 못 거르는
알레르기를 안고 그렇게 말하는 건 더 위험하다.

**제안(진웅 판단 필요)**: 하드 제약의 목적을 "거르기"가 아니라 **"결정 순간까지
들고 가기"**로 다시 잡는다. 코스 카드에 "다솜님은 땅콩 알레르기가 있어요.
이 가게 메뉴는 확인하지 못했어요"를 붙인다. 장소 데이터가 0이어도 지금 동작하고,
정직하고, 커플이 가게에서 직접 확인한다. 규칙 5의 "점수와 거래되지 않는다"는
그대로 지켜진다 — 술어를 아직 평가하지 못한다고 인정할 뿐이다.

`shared/openingHours.ts`가 이 문제의 작은 판본을 이미 이렇게 풀었다.
`isOpenDuring`은 `open | closed | **unknown**` 셋을 돌려주고, 모름을 열림으로
접지 않는다.

**예산은 여전히 같은 구멍이다.** 음식점·카페의 `price_level`이 전부 NULL이라
"예산 5만원"을 걸어도 판정할 근거가 없다. 영업시간은 2026-08-12 TourAPI detail
수집으로 880건에 원문 또는 파싱 결과가 생겼고, 닫힘은 실제로 제외할 수 있게 됐다.
다만 파싱 불가·미상은 코스에 확인 경고를 붙인다(`#29`).

### Q17. 오늘의 질문이 만든 신호는 언제까지 유효한가

온보딩을 붙이면서 드러났다 (`03-decisions.md` #26). `preference_signals`는 지금
**수명이 없다.** 오늘의 질문에 "이번엔 실내"라고 답하면 그 약속이 지나고 두 달 뒤에도
`indoor = +1`이 그대로 남아 모든 추천에 들어간다.

온보딩 값(평소 성향)과 오늘의 질문 값(그날의 기분)이 같은 행을 두고 다투는데,
지금 규칙은 "더 최근에 명시적으로 답한 쪽이 이긴다" 하나뿐이다. 그래서 3주 전 하루의
기분이 방금 고친 평소 성향을 이기는 상황은 없지만, **아무도 손대지 않으면 3주 전
기분이 계속 살아 있다.**

세 갈래:
1. 약속이 지나면 그 약속에서 나온 신호를 온보딩 값으로 되돌린다 — `date_question_answers`에
   원본이 있어서 가능하다. 되돌릴 시점을 정하는 배치가 필요하다
2. `preference_signals`에 `expires_at`이나 감쇠 가중치를 둔다 — 점수식이 복잡해진다
3. 그냥 둔다 — L0에서는 신호가 몇 개 없어서 티가 안 난다

**세션 100개가 모이기 전에는 판단 근거가 없다.** 지금은 3번으로 두고, 로그에
`ranker_version`이 남으므로 나중에 오프라인에서 비교할 수 있다.

### Q18. 영화 상영시간표를 어떻게 넣을 것인가 — **지금은 못 한다**

"몇 시부터 몇 시까지 새로 나온 영화"는 코스의 핵심 요구인데, **극장별 상영시간표를
주는 공개 API가 없다.** 영진위는 박스오피스(무엇이 흥행 중인가)만 주고,
CGV·롯데·메가박스는 공개 API가 없다. 크롤링은 유지보수 부담과 약관이 걸린다.

**지금 쓰는 우회**: 슬롯을 "영화"가 아니라 **"영화관, 2시간"**으로 잡는다.
코스에는 "16:00–18:10 영화관"까지 나오고 무슨 영화를 볼지는 둘이 고른다.
실제 커플도 그렇게 하고, 앱이 영화까지 정하면 오히려 방해가 된다.

박스오피스로 "이번 주 새로 나온 영화" 목록만 옆에 붙이는 건 가능하다.
진웅이 API를 찾아보는 중(2026-08-11).

### Q19. 팝업스토어가 0건이다

`kind='popup'`인 행이 **하나도 없다** (음식점 458 · 활동 569 · 전시 223 ·
카페 107 · 공원 7 = 1,364). 공개 API가 없어서(#13) 결국 손으로 넣어야 한다.

이게 지금 가장 아픈 구멍이다. 소곤.zip이 차별화하겠다는 축이 전시·팝업인데
팝업이 0건이고, "사용자가 좋아하는 캐릭터 팝업"은 매칭할 대상 자체가 없다.
IP·캐릭터는 팝업이 50건쯤 쌓이면 태그 하나로 붙는다. 순서가 반대일 수 없다.

### Q20. 서비스 시간대 08:00~23:00을 언제까지 쓸 것인가

`dateCourseSkeleton.ts`의 `SERVICE_START_MINUTES`/`SERVICE_END_MINUTES`는
**추측이다.** 영업시간이 전부 NULL이라 "새벽 2시에 문 연 카페"를 못 거르니,
아예 그 시간대에 슬롯을 안 만든다. 없으면 00:00~12:45 창에 02:15 카페가 들어간다.

`tourApiDetail.mjs`로 880건의 영업시간 원문 또는 파싱 결과를 채웠고 후보 단계에서
`closed`를 제외하기 시작했다. 아직 파싱 불가·미상 후보가 있으므로 이 상수를 지울지,
아니면 `unknown` 장소를 위한 서비스 경계로 남길지가 질문이다.

### ~~Q4. 서비스 지역을 어디로 한정할까~~ → 답 나옴 (`03-decisions.md` #13)

서울 핵심 상권 6곳: 성수 · 한남 · 연남/홍대 · 을지로 · 잠실 · 강남/신사.

### Q11. 반려동물 동반을 하드 제약으로 넣을 것인가

진웅 제안 (2026-08-10). 커플이 강아지를 데려가면 **동반 불가 장소는 점수와 무관하게
못 간다.** 알레르기·영업시간과 같은 층이다 (절대 규칙 5).

넣는다면:
- `preference_signals`에 `axis='travel'`, `tag='pet'`, `is_hard_constraint=1`로 들어간다.
  스키마 변경은 필요 없다.
- **장소 쪽에 동반 가능 여부를 담을 자리가 없다.** `tags_json`에 `'반려동물동반'`을
  넣는 방법과 컬럼을 새로 파는 방법이 있는데, 하드 필터는 인덱스를 타야 해서
  태그 배열 안을 뒤지는 건 느리다. 0004에서 `pet_allowed INTEGER`를 다는 쪽이 맞아 보인다.
- TourAPI '반려동물 동반 여행 정보'로 채운다. **카카오·서울시 데이터에는 이 정보가 없다.**
  즉 대부분의 장소가 '미상'이고, 미상을 제외하면 후보가 거의 안 남는다.
  미상을 어떻게 다룰지가 진짜 질문이다 — 알레르기와 달리 "모르면 일단 보여주고
  확인 필요라고 표시"가 가능할 수도 있다.

TourAPI 활용신청에 이 기능을 포함시켜 뒀다.

### Q12. 1박 이상 일정을 다룰 것인가

TourAPI 숙박정보조회를 신청해뒀지만 L0에서는 쓰지 않는다. 코스가 하루 단위를
넘어가면 이동·예산·시간 계산이 통째로 달라진다. 커플 여행으로 넓힐 때 다시 본다.

### ~~Q5. 병합 키가 서로 다른 공연을 같은 장소로 본다~~ → 고침 (`03-decisions.md` #15)

괄호 안을 남기고, 병합 키에 기간 겹침을 넣었다. 프로덕션 충돌 12건 → 0건.
남은 한 가지는 아래 Q13으로 옮겼다.

### Q13. 병합 임계값 50m / 2km이 맞나

감으로 정한 값이다. Q5를 고치느라 미뤄뒀다. 지금은 판단할 근거가 없다 —
운영자 등록이 실제로 쓰이기 시작하고 오탐·미탐이 쌓여야 조정할 수 있다.

같이 볼 것: **한쪽 소스가 괄호 부분을 통째로 빠뜨리면 갈라진다.**
실데이터에 이미 한 쌍 있다.

```
공근혜갤러리
[K드라마 촬영지] 공근혜갤러리   ← 같은 곳인데 이제 다른 키
```

지금은 수집기가 결정론적 id를 써서 어차피 두 행이라 손해가 없지만,
카카오가 붙으면 같은 가게를 소스마다 다르게 적는 경우가 늘어난다.
그때 **한쪽 이름이 다른 쪽에 통째로 포함되면 병합 검토(409 아님)로 올리는**
안을 검토한다. 자동 병합이 아니라 사람이 보는 큐로 보내는 거라 오탐 비용이 싸다.

---

## 제품

### Q6. 커플과 친구를 동시에 지원할 것인가, 커플부터 할 것인가

`relationship_type`은 이미 있지만 실제로 다르게 동작하는 부분이 없다.
추천 로직은 둘이 꽤 다를 수 있다 (친구는 예산·거리 제약이 더 느슨하다).

### Q7. 최고 민감도 파일은 "추천에 반영하기"를 기본 꺼둘 것인가

지금은 `recommendation_on` 기본값이 1이다. 민감한 내용일수록 취향 반영도 조심스러운 게
맞을 수 있는데, 그러면 추천 데이터가 더 줄어든다.

### Q8. 열림 시점을 달력 날짜 기준으로 갈 것인가, 관계 D+ 기준으로 갈 것인가

지금은 둘 다 지원한다(`100일 후`는 D+, `직접 날짜 선택`은 달력).
UI에서 하나를 주(主)로 밀어야 한다면 어느 쪽인가.

**#20 이후 이 질문의 무게가 커졌다.** 날짜가 1급 객체가 됐는데 날짜를 세는
방식이 두 가지면, 데이트 약속과 소곤파일이 같은 달력 위에 안 놓인다.

### Q15. 데이트 약속을 얼마나 앞서 잡게 할 것인가

`03-decisions.md` #20으로 날짜가 1급 객체가 됐는데, 얼마나 먼 날짜까지 허용할지가
안 정해졌다. 이게 두 가지를 좌우한다.

- **오늘의 질문 개수** — D-7부터 매일이면 7문항이다. 한 달 전에 잡으면 30문항이
  되는데 그만큼 물어볼 게 있는지, 매일 답하는 게 부담인지는 해봐야 안다
- **코스 신선도** — 멀수록 전시가 그 전에 끝난다. 지금 데이터로 보면 30일 안에
  224건이 끝난다. 2주를 넘기면 미리 뽑은 코스의 상당수가 죽는다

첫 안: **최대 4주, 질문은 D-7부터.** 그 앞은 "날짜만 찍어두기".

**0004 구현 메모**: 상한은 아직 강제하지 않았다. 질문은 D-7~D-1에만 열리고,
그보다 먼 약속은 날짜만 저장된다 (`03-decisions.md` #23). 베타 사용량을 본 뒤
4주 상한이 정말 필요한지 결정한다.

### Q16. 소곤파일과 데이트 약속을 같은 날짜에 묶어 보여줄 것인가

#20이 "둘 다 날짜 위에 놓인다"고 정했지만, 화면에서 하나로 합칠지는 별개다.

합치면 서사가 강해진다 — "12월 24일: 소곤파일 1개가 열리고, 코스가 준비돼 있어요."
다만 **절대 규칙 3**(열림은 항상 명시적이다)과 부딪히지 않게 조심해야 한다.
같은 카드에 놓으면 코스를 여는 동작이 소곤파일을 여는 것처럼 보일 수 있다.
"비공개 내용이 자동으로 공개되는 것처럼 **보이는** 화면도 만들지 않는다"가
`00-product.md`의 UX 원칙이다.

---

## 운영

### Q9. 베타 계정을 친구가 직접 만들게 할 것인가, 진웅이 미리 만들어줄 것인가

### Q10. 운영자가 베타 데이터를 어디까지 볼 수 있어야 하는가

절대 규칙상 소곤파일 본문은 못 본다. 그럼 무엇을 봐야 운영이 되는가 —
가입 수, 연결 수, 파일 개수, 추천 저장률 정도인가.

---

## 답이 나온 질문

`03-decisions.md`로 옮긴 것들. 취소선으로 위에 남겨둔다 — Q1 · Q2 · Q3 · Q4 · Q5.
