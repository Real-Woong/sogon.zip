export function ZipBadge() {
  return (
    <span className="inline-flex items-center px-2 py-0.5 rounded-md bg-[color:var(--lavender-light)] text-[color:var(--lavender)] text-xs font-medium">
      .zip
    </span>
  );
}
