export function ZipBadge() {
  return (
    <span className="inline-flex shrink-0 items-center rounded-md bg-[color:var(--lavender-light)] px-2 py-0.5 text-xs font-medium text-[color:var(--lavender)]">
      .zip
    </span>
  );
}
